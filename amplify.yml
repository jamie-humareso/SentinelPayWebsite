version: 1
# Next.js standard build configuration for AWS Amplify - v2.1
# This configuration uses .next directory for standard Next.js builds
# BuildSpec has been updated in Amplify console
frontend:
  phases:
    preBuild:
      commands:
        - echo "=== AMPLIFY BUILD START ==="
        - echo "Installing dependencies..."
        - npm ci
        - echo "Dependencies installed successfully"
    build:
      commands:
        - echo "Building Next.js application..."
        - npm run build
        - echo "Build completed successfully"
        - echo "=== AMPLIFY BUILD END ==="
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
